package app.vehplayer.android.dashboard

import androidx.fragment.app.Fragment
import app.vehplayer.android.R

/**
 * Page 1 of heroPager. Still the static "Nothing playing" placeholder from
 * Phase 1 - real media info (MediaSessionManager + Notification Listener
 * access) is Phase 2, not part of this pass.
 */
class NowPlayingFragment : Fragment(R.layout.fragment_now_playing)
